# backend

## Swagger API Documentation

After installing dependencies and starting the server (`npm run dev`),
visit `http://localhost:3000/api-docs` to view the Swagger UI with the
current OpenAPI specification. The docs are generated from JSDoc comments
in the route files using `swagger-jsdoc` and served via `swagger-ui-express`.
