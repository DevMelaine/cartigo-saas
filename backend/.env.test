# copy your development DATABASE_URL and change to point at a test database
# e.g. postgresql://user:pass@localhost:5432/myapp_test
DATABASE_URL="postgresql://postgres:1234@localhost:5000/cartigo_saas"
set NODE_ENV=test
# you may also override JWT secret here for deterministic tokens
JWT_SECRET="test-secret"
